import { useEffect, useMemo, useState } from "react";
import { Minus, Plus, Search, ShoppingBag, X } from "lucide-react";
import { LangToggle } from "@/components/lang-toggle";
import { MenuBadge, MenuMedia, MenuPrice } from "@/components/menu";
import { useLang } from "@/lib/lang";
import { getGuestSessionId } from "@/lib/menu/session";
import { recordPublicEvent } from "@/lib/menu/public";
import { submitPublicOrder } from "@/lib/menu/order-public";
import type { Lang, Product, ProductOptions, PublicMenu } from "@/lib/menu/types";
import { cn, formatSar } from "@/lib/utils";

const text = (lang: Lang, ar: string, en: string) => lang === "ar" ? ar || en : en || ar;
type CartItem = { key: string; product: Product; variantId: string; modifierOptionIds: string[]; unitPrice: number; quantity: number };

function ProductSheet({ lang, product, options, close, add }: { lang: Lang; product: Product; options?: ProductOptions; close: () => void; add: (item: CartItem) => void }) {
  const variants = options?.variants.filter((v) => v.isAvailable) ?? [];
  const groups = options?.groups.filter((g) => g.isActive) ?? [];
  const [variantId, setVariantId] = useState(variants[0]?.id ?? "");
  const [selected, setSelected] = useState<string[]>([]);
  const [error, setError] = useState("");
  const base = variants.find((v) => v.id === variantId)?.price ?? product.price;
  const extra = (options?.options ?? []).filter((o) => selected.includes(o.id)).reduce((sum, o) => sum + o.priceDelta, 0);
  const toggle = (groupId: string, id: string, max: number) => setSelected((current) => current.includes(id) ? current.filter((x) => x !== id) : current.filter((x) => (options?.options ?? []).some((o) => o.id === x && o.groupId === groupId)).length >= max ? current : [...current, id]);
  const confirm = () => {
    for (const group of groups) {
      const count = selected.filter((id) => (options?.options ?? []).some((o) => o.id === id && o.groupId === group.id)).length;
      if (count < group.minSelect || count > group.maxSelect) { setError(text(lang, `أكمل اختيار «${group.nameAr}»`, `Complete “${group.nameEn || group.nameAr}”`)); return; }
    }
    add({ key: `${product.id}:${variantId}:${[...selected].sort().join(",")}`, product, variantId, modifierOptionIds: [...selected].sort(), unitPrice: base + extra, quantity: 1 });
    close();
  };
  return <div className="fixed inset-0 z-50 grid items-end bg-black/45 sm:items-center sm:p-4" role="presentation" onMouseDown={(e) => e.target === e.currentTarget && close()}><section role="dialog" aria-modal="true" aria-labelledby="fast-product-title" className="max-h-[92dvh] w-full overflow-y-auto rounded-t-[2rem] bg-paper shadow-2xl sm:mx-auto sm:max-w-xl sm:rounded-[2rem]"><header className="flex items-center justify-between border-b border-line px-5 py-4"><span className="text-sm font-semibold">{text(lang, "تخصيص الطلب", "Customize")}</span><button type="button" onClick={close} aria-label={text(lang, "إغلاق", "Close")} className="grid size-10 place-items-center rounded-full hover:bg-sand"><X className="size-5" /></button></header><div className="grid gap-5 p-5"><MenuMedia src={product.imageUrl} className="aspect-square w-full rounded-[1.5rem]" eager /><div><h2 id="fast-product-title" className="text-2xl font-semibold">{text(lang, product.nameAr, product.nameEn)}</h2><MenuPrice price={base + extra} currency={product.currency} lang={lang} className="mt-1 text-lg" />{product.descriptionAr || product.descriptionEn ? <p className="mt-2 text-sm leading-6 text-muted">{text(lang, product.descriptionAr, product.descriptionEn)}</p> : null}</div>{product.dietaryLabels.length ? <div className="flex flex-wrap gap-2">{product.dietaryLabels.map((x) => <MenuBadge key={x} tone="muted">{x}</MenuBadge>)}</div> : null}{variants.length ? <fieldset className="grid gap-2 border-t border-line pt-4"><legend className="text-sm font-semibold">{text(lang, "الحجم", "Size")}</legend>{variants.map((v) => <label key={v.id} className={cn("flex cursor-pointer items-center justify-between rounded-xl border p-3", variantId === v.id ? "border-ink bg-sand" : "border-line")}><span className="flex items-center gap-2"><input type="radio" name={`fast-size-${product.id}`} checked={variantId === v.id} onChange={() => setVariantId(v.id)} />{text(lang, v.nameAr, v.nameEn)}</span><MenuPrice price={v.price} currency={product.currency} lang={lang} /></label>)}</fieldset> : null}{groups.map((group) => <fieldset key={group.id} className="grid gap-2 border-t border-line pt-4"><legend className="text-sm font-semibold">{text(lang, group.nameAr, group.nameEn)} {group.isRequired ? "*" : ""}</legend>{(options?.options ?? []).filter((o) => o.groupId === group.id && o.isAvailable).map((option) => <label key={option.id} className="flex cursor-pointer items-center justify-between rounded-xl border border-line p-3"><span className="flex items-center gap-2"><input type={group.maxSelect === 1 ? "radio" : "checkbox"} name={`fast-modifier-${product.id}-${group.id}`} checked={selected.includes(option.id)} onChange={() => toggle(group.id, option.id, group.maxSelect)} />{text(lang, option.nameAr, option.nameEn)}</span><span className="text-sm text-accent">{option.priceDelta === 0 ? "—" : `${option.priceDelta > 0 ? "+" : ""}${formatSar(option.priceDelta, lang)}`}</span></label>)}</fieldset>)}{error ? <p role="alert" className="rounded-xl bg-bad/10 p-3 text-sm text-bad">{error}</p> : null}<button type="button" onClick={confirm} className="min-h-12 rounded-xl bg-ink px-5 font-semibold text-paper">{text(lang, `أضف · ${formatSar(base + extra, lang)}`, `Add · ${formatSar(base + extra, lang)}`)}</button></div></section></div>;
}

function Cart({ lang, items, setItems, close, submit, error, submitting }: { lang: Lang; items: CartItem[]; setItems: (items: CartItem[]) => void; close: () => void; submit: (customer: { name: string; phone: string; email: string; notes: string }) => void; error: string; submitting: boolean }) {
  const [name, setName] = useState(""); const [phone, setPhone] = useState(""); const [notes, setNotes] = useState("");
  const total = items.reduce((s, x) => s + x.unitPrice * x.quantity, 0);
  const change = (key: string, delta: number) => setItems(items.flatMap((x) => x.key !== key ? [x] : x.quantity + delta <= 0 ? [] : [{ ...x, quantity: Math.min(20, x.quantity + delta) }]));
  return <div className="fixed inset-0 z-[60] bg-black/45" role="presentation" onMouseDown={(e) => e.target === e.currentTarget && close()}><section role="dialog" aria-modal="true" aria-labelledby="fast-cart-title" className="ms-auto flex h-full w-full max-w-md flex-col bg-paper shadow-2xl"><header className="flex items-center justify-between border-b border-line px-5 py-4"><div><h2 id="fast-cart-title" className="font-semibold">{text(lang, "سلة الطلب", "Your order")}</h2><p className="text-xs text-muted">{items.reduce((s, x) => s + x.quantity, 0)} {text(lang, "صنف", "items")}</p></div><button type="button" onClick={close} aria-label={text(lang, "إغلاق", "Close")} className="grid size-10 place-items-center rounded-full hover:bg-sand"><X className="size-5" /></button></header><div className="flex-1 space-y-3 overflow-y-auto p-5">{items.map((item) => <article key={item.key} className="rounded-2xl border border-line p-3"><div className="flex gap-3"><MenuMedia src={item.product.imageUrl} className="size-16 rounded-xl" /><div className="min-w-0 flex-1"><p className="font-semibold">{text(lang, item.product.nameAr, item.product.nameEn)}</p><p className="text-sm text-accent">{formatSar(item.unitPrice * item.quantity, lang)}</p></div></div><div className="mt-3 flex items-center justify-end gap-2"><button type="button" onClick={() => change(item.key, -1)} aria-label={text(lang, "تقليل", "Decrease")} className="grid size-9 place-items-center rounded-lg border"><Minus className="size-4" /></button><span>{item.quantity}</span><button type="button" onClick={() => change(item.key, 1)} aria-label={text(lang, "زيادة", "Increase")} className="grid size-9 place-items-center rounded-lg border"><Plus className="size-4" /></button></div></article>)}<div className="grid gap-3 border-t border-line pt-4"><input value={name} onChange={(e) => setName(e.target.value)} placeholder={text(lang, "الاسم *", "Name *")} className="h-11 rounded-xl border border-line px-3" /><input value={phone} onChange={(e) => setPhone(e.target.value)} inputMode="tel" placeholder={text(lang, "رقم الجوال *", "Phone *")} className="h-11 rounded-xl border border-line px-3" /><textarea value={notes} onChange={(e) => setNotes(e.target.value)} placeholder={text(lang, "ملاحظات", "Notes")} className="min-h-24 rounded-xl border border-line p-3" />{error ? <p role="alert" className="rounded-xl bg-bad/10 p-3 text-sm text-bad">{error}</p> : null}</div></div><footer className="border-t border-line p-5"><div className="mb-3 flex justify-between"><span className="text-sm text-muted">{text(lang, "الإجمالي", "Total")}</span><strong className="text-lg text-accent">{formatSar(total, lang)}</strong></div><button type="button" disabled={submitting || !items.length || name.trim().length < 2 || phone.trim().length < 8} onClick={() => submit({ name, phone, email: "", notes })} className="h-12 w-full rounded-xl bg-ink font-semibold text-paper disabled:opacity-50">{submitting ? text(lang, "جاري الإرسال…", "Submitting…") : text(lang, "إرسال الطلب", "Submit order")}</button></footer></section></div>;
}

export function FastCasualTemplate({ menu, preview = false }: { menu: PublicMenu; preview?: boolean }) {
  const { lang } = useLang(); const { tenant, branch, categories, products, productOptions } = menu;
  const [query, setQuery] = useState(""); const [categoryId, setCategoryId] = useState("all"); const [selectedId, setSelectedId] = useState<string | null>(null); const [cart, setCart] = useState<CartItem[]>([]); const [cartOpen, setCartOpen] = useState(false); const [submitting, setSubmitting] = useState(false); const [error, setError] = useState(""); const [success, setSuccess] = useState<number | null>(null);
  const visible = preview ? products : products.filter((p) => p.isAvailable);
  const featured = visible.filter((p) => p.isFeatured);
  const filtered = useMemo(() => { const q = query.trim().toLowerCase(); return visible.filter((p) => (categoryId === "all" || p.categoryId === categoryId) && (!q || [p.nameAr, p.nameEn, p.descriptionAr, p.descriptionEn, ...p.tags].some((x) => x.toLowerCase().includes(q)))); }, [visible, query, categoryId]);
  useEffect(() => { if (!preview) void recordPublicEvent({ data: { slug: tenant.slug, branchSlug: branch.slug, eventType: "visit", lang, sessionId: getGuestSessionId() } }); }, [tenant.slug, branch.slug, lang, preview]);
  const add = (item: CartItem) => setCart((current) => { const found = current.find((x) => x.key === item.key); return found ? current.map((x) => x.key === item.key ? { ...x, quantity: Math.min(20, x.quantity + 1) } : x) : [...current, item]; });
  const submit = async (customer: { name: string; phone: string; email: string; notes: string }) => { setSubmitting(true); setError(""); const result = await submitPublicOrder({ data: { slug: tenant.slug, branchSlug: branch.slug, source: "web", customerName: customer.name, customerPhone: customer.phone, customerEmail: customer.email, notes: customer.notes, items: cart.map((x) => ({ productId: x.product.id, quantity: x.quantity, selected: { variantId: x.variantId || null, modifierOptionIds: x.modifierOptionIds } })) } }); setSubmitting(false); if (!result.ok) { setError(result.error); return; } setCart([]); setCartOpen(false); setSuccess(result.data.orderNumber); };
  return <div className="min-h-dvh bg-paper text-ink"><header className="bg-ink text-paper"><div className="mx-auto grid max-w-3xl gap-5 px-5 pb-7 pt-6"><div className="flex items-start justify-between gap-4"><div className="flex min-w-0 items-center gap-3">{tenant.logoUrl ? <img src={tenant.logoUrl} alt="" className="size-14 rounded-2xl object-cover" /> : <div className="grid size-14 place-items-center rounded-2xl bg-accent text-xl font-bold">{text(lang, tenant.nameAr, tenant.nameEn).slice(0, 1)}</div>}<div className="min-w-0"><p className="text-xs font-medium uppercase tracking-[0.16em] text-paper/60">{tenant.city}</p><h1 className="truncate font-display text-2xl font-bold">{text(lang, tenant.nameAr, tenant.nameEn)}</h1></div></div><div className="rounded-full bg-paper/10"><LangToggle /></div></div>{tenant.taglineAr || tenant.taglineEn ? <p className="max-w-prose text-sm leading-6 text-paper/70">{text(lang, tenant.taglineAr, tenant.taglineEn)}</p> : null}</div></header>
    <main className="mx-auto grid max-w-3xl gap-7 px-5 py-6"><section className="grid gap-4"><div className="flex items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-accent">{text(lang, "الأكثر طلباً", "Most wanted")}</p><h2 className="mt-1 text-2xl font-black tracking-tight">{text(lang, "اطلبها بسرعة", "Order it fast")}</h2></div>{cart.length ? <button type="button" onClick={() => setCartOpen(true)} aria-label={text(lang, "فتح السلة", "Open cart")} className="inline-flex min-h-11 items-center gap-2 rounded-full bg-accent px-4 text-sm font-bold text-white"><ShoppingBag className="size-4" />{cart.reduce((s, x) => s + x.quantity, 0)}</button> : null}</div><label className="relative block"><Search className="pointer-events-none absolute start-4 top-1/2 size-5 -translate-y-1/2 text-muted" /><input value={query} onChange={(e) => setQuery(e.target.value)} aria-label={text(lang, "بحث في المنيو", "Search menu")} placeholder={text(lang, "ابحث عن برجر، بيتزا…", "Search burgers, pizza…")} className="h-13 w-full rounded-2xl border-2 border-line bg-paper ps-12 pe-4 outline-none focus:border-accent" /></label><nav className="flex gap-2 overflow-x-auto pb-1" aria-label={text(lang, "أقسام المنيو", "Menu categories")}><button type="button" onClick={() => setCategoryId("all")} className={cn("shrink-0 rounded-full px-4 py-2 text-sm font-semibold", categoryId === "all" ? "bg-ink text-paper" : "bg-sand text-muted")}>{text(lang, "الكل", "All")}</button>{categories.filter((c) => c.isActive).map((c) => <button type="button" key={c.id} onClick={() => setCategoryId(c.id)} className={cn("shrink-0 rounded-full px-4 py-2 text-sm font-semibold", categoryId === c.id ? "bg-ink text-paper" : "bg-sand text-muted")}>{text(lang, c.nameAr, c.nameEn)}</button>)}</nav></section>
      {featured.length && categoryId === "all" && !query ? <section className="grid gap-3"><div className="flex items-center justify-between"><h2 className="text-lg font-bold">{text(lang, "اختياراتنا", "Our picks")}</h2><span className="text-xs text-muted">{featured.length}</span></div><div className="flex gap-3 overflow-x-auto pb-1">{featured.slice(0, 5).map((p) => <button type="button" key={p.id} onClick={() => setSelectedId(p.id)} className="w-44 shrink-0 overflow-hidden rounded-2xl border border-line bg-paper text-start shadow-sm"><MenuMedia src={p.imageUrl} className="aspect-square w-full" /><div className="p-3"><p className="line-clamp-1 font-semibold">{text(lang, p.nameAr, p.nameEn)}</p><MenuPrice price={p.price} currency={p.currency} lang={lang} className="mt-1 text-sm text-accent" /></div></button>)}</div></section> : null}
      <section className="grid gap-3">{filtered.map((p) => <article key={p.id} className="flex gap-3 rounded-2xl border border-line bg-paper p-3"><button type="button" onClick={() => setSelectedId(p.id)} className="flex min-w-0 flex-1 gap-3 text-start"><MenuMedia src={p.imageUrl} className="size-24 shrink-0 rounded-xl" /><span className="min-w-0"><span className="line-clamp-2 font-bold">{text(lang, p.nameAr, p.nameEn)}</span>{p.descriptionAr || p.descriptionEn ? <span className="mt-1 line-clamp-2 block text-xs leading-5 text-muted">{text(lang, p.descriptionAr, p.descriptionEn)}</span> : null}<span className="mt-2 block"><MenuPrice price={p.price} currency={p.currency} lang={lang} className="font-bold text-accent" /></span></button><button type="button" onClick={() => setSelectedId(p.id)} aria-label={text(lang, `إضافة ${p.nameAr}`, `Add ${p.nameEn || p.nameAr}`)} className="self-end grid size-10 shrink-0 place-items-center rounded-xl bg-accent text-white"><Plus className="size-5" /></button></article>)}</section></main>
    {selectedId ? <ProductSheet lang={lang} product={products.find((p) => p.id === selectedId)!} options={productOptions?.[selectedId]} close={() => setSelectedId(null)} add={add} /> : null}
    {cartOpen ? <Cart lang={lang} items={cart} setItems={setCart} close={() => setCartOpen(false)} submit={submit} error={error} submitting={submitting} /> : null}
    {success ? <div className="fixed inset-x-4 bottom-5 z-[70] mx-auto max-w-md rounded-2xl bg-ink p-4 text-paper shadow-2xl" role="status"><p className="font-bold">{text(lang, "تم استلام طلبك", "Order received")}</p><p className="mt-1 text-sm text-paper/70">{text(lang, `رقم الطلب: ${success}`, `Order #${success}`)}</p><button type="button" onClick={() => setSuccess(null)} className="mt-3 rounded-lg bg-paper/10 px-3 py-2 text-sm">{text(lang, "إغلاق", "Close")}</button></div> : null}
  </div>;
}
