import { useEffect, useMemo, useState } from "react";
import { CheckCircle2, Copy, ExternalLink, Mail, MessageCircle, Phone, RefreshCw, Search, ShieldCheck, UserCheck, XCircle } from "lucide-react";
import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { LangToggle } from "@/components/lang-toggle";
import { useCurrentUserState } from "@/lib/auth/use-current-user";
import { useLang } from "@/lib/lang";
import { getPlatformCustomerRequests, updatePlatformCustomerRequest, type PlatformCustomerRequest } from "@/lib/menu/platform-customers";
export const Route = createFileRoute("/admin/onboarding")({ component: AdminOnboardingPage });
type Status = PlatformCustomerRequest["status"] | "all";
const labels = { ar: { pending: "بانتظار الاعتماد", approved: "معتمد", rejected: "مرفوض", all: "كل العملاء", title: "طلبات العملاء الجدد", subtitle: "اعتماد العملاء الجدد: مركز موحد لمراجعة العملاء واعتمادهم دون خلطهم مع طلبات الطعام.", refresh: "تحديث", search: "ابحث باسم المطعم أو المدينة أو الجوال أو البريد", empty: "لا توجد طلبات مطابقة.", details: "تفاصيل العميل", business: "المطعم", city: "المدينة", phone: "الجوال", email: "البريد", submitted: "تاريخ الطلب", status: "الحالة", notes: "ملاحظات الإدارة", notesPlaceholder: "ملاحظة داخلية…", approve: "اعتماد العميل", reject: "رفض الطلب", approvedMessage: "تم اعتماد العميل", rejectedMessage: "تم رفض الطلب", menu: "فتح المنيو", copy: "نسخ الرابط", copied: "تم النسخ", call: "اتصال", whatsapp: "WhatsApp", emailAction: "بريد", overview: "الإجمالي", active: "الحساب نشط", published: "المنيو منشور", draft: "المنيو غير منشور" }, en: { pending: "Pending", approved: "Approved", rejected: "Rejected", all: "All customers", title: "New Customer Requests", subtitle: "New customer approval: one workspace for reviewing and approving customers without mixing them with food orders.", refresh: "Refresh", search: "Search restaurant, city, phone, or email", empty: "No matching requests.", details: "Customer details", business: "Restaurant", city: "City", phone: "Phone", email: "Email", submitted: "Submitted", status: "Status", notes: "Admin notes", notesPlaceholder: "Internal note…", approve: "Approve customer", reject: "Reject request", approvedMessage: "Customer approved", rejectedMessage: "Request rejected", menu: "Open menu", copy: "Copy link", copied: "Copied", call: "Call", whatsapp: "WhatsApp", emailAction: "Email", overview: "Total", active: "Account active", published: "Menu published", draft: "Menu not published" } } as const;
function AdminOnboardingPage() { const navigate=useNavigate(); const {lang}=useLang(); const copy=labels[lang]; const {user,isPending}=useCurrentUserState(); const [rows,setRows]=useState<PlatformCustomerRequest[]>([]); const [selectedId,setSelectedId]=useState<string|null>(null); const [status,setStatus]=useState<Status>("all"); const [query,setQuery]=useState(""); const [notes,setNotes]=useState(""); const [loading,setLoading]=useState(true); const [saving,setSaving]=useState(false); const [copied,setCopied]=useState(false); const [error,setError]=useState(""); const load=async()=>{if(!user)return;setLoading(true);setError("");try{const result=await getPlatformCustomerRequests({data:{status:status==="all"?undefined:status,q:query.trim()||undefined}});if(!result.ok)setError(result.error);else{setRows(result.data);setSelectedId(current=>current&&result.data.some(r=>r.id===current)?current:result.data[0]?.id??null);}}catch(err){setError(err instanceof Error?err.message:"تعذر تحميل الطلبات")}finally{setLoading(false)}}; useEffect(()=>{if(isPending)return;if(!user){void navigate({to:"/login",search:{redirect:"/admin/onboarding"} as never,replace:true});return;}void load()},[isPending,user]); useEffect(()=>{if(!isPending&&user){const timer=window.setTimeout(()=>void load(),250);return()=>window.clearTimeout(timer)}},[status,query]); const selected=useMemo(()=>rows.find(r=>r.id===selectedId)??null,[rows,selectedId]); useEffect(()=>{setNotes(selected?.adminNotes??"");setCopied(false)},[selected?.id]); const counts=useMemo(()=>({pending:rows.filter(r=>r.status==="pending").length,approved:rows.filter(r=>r.status==="approved").length,rejected:rows.filter(r=>r.status==="rejected").length}),[rows]); async function setCustomerStatus(next:"approved"|"rejected"){if(!selected||saving)return;setSaving(true);setError("");try{const result=await updatePlatformCustomerRequest({data:{id:selected.id,status:next,adminNotes:notes}});if(!result.ok)setError(result.error);else setRows(current=>current.map(row=>row.id===result.data.id?result.data:row))}catch(err){setError(err instanceof Error?err.message:"تعذر تحديث الطلب")}finally{setSaving(false)}} async function copyMenu(){if(!selected)return;await navigator.clipboard.writeText(`${window.location.origin}${selected.menuUrl}`);setCopied(true);window.setTimeout(()=>setCopied(false),1600)} if(isPending||!user)return <div className="grid min-h-[60vh] place-items-center text-sm text-muted">{lang==="ar"?"جارٍ التحقق من صلاحيات مالك المنصة…":"Checking platform-owner access…"}</div>; return <main className="mx-auto grid max-w-[1500px] gap-5 py-4 lg:py-8" dir={lang==="ar"?"rtl":"ltr"}><header className="rounded-3xl border border-line bg-paper p-5 md:p-7"><div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div><div className="inline-flex items-center gap-2 rounded-full border border-line bg-sand/50 px-3 py-1 text-xs text-muted"><ShieldCheck className="size-3.5"/> Menu V3 · Platform Owner</div><h1 className="mt-3 font-display text-3xl font-semibold tracking-tight md:text-4xl">{copy.title}</h1><p className="mt-2 max-w-4xl text-sm leading-6 text-muted">{copy.subtitle}</p></div><div className="flex flex-wrap items-center gap-2"><LangToggle/><Button variant="outline" onClick={()=>void navigate({to:"/admin"})}>لوحة المنصة</Button><Button variant="outline" disabled={loading} onClick={()=>void load()}><RefreshCw className={loading?"size-4 animate-spin":"size-4"}/>{copy.refresh}</Button></div></div></header><section className="grid grid-cols-3 gap-3"><Metric label={copy.pending} value={counts.pending} active={status==="pending"} onClick={()=>setStatus("pending")}/><Metric label={copy.approved} value={counts.approved} active={status==="approved"} onClick={()=>setStatus("approved")}/><Metric label={copy.rejected} value={counts.rejected} active={status==="rejected"} onClick={()=>setStatus("rejected")}/></section>{error?<div className="rounded-2xl border border-bad/30 bg-bad/5 px-4 py-3 text-sm text-bad" role="alert">{error}</div>:null}<section className="grid min-h-[620px] gap-4 lg:grid-cols-[minmax(0,1.05fr)_minmax(360px,.95fr)]"><div className="grid content-start gap-3 rounded-3xl border border-line bg-sand/20 p-3"><div className="grid gap-2 md:grid-cols-[1fr_180px]"><label className="relative"><Search className="pointer-events-none absolute start-3 top-1/2 size-4 -translate-y-1/2 text-muted"/><Input className="ps-9" value={query} onChange={e=>setQuery(e.target.value)} placeholder={copy.search}/></label><select value={status} onChange={e=>setStatus(e.target.value as Status)} className="h-10 rounded-md border border-line bg-paper px-3 text-sm"><option value="all">{copy.all}</option><option value="pending">{copy.pending}</option><option value="approved">{copy.approved}</option><option value="rejected">{copy.rejected}</option></select></div><div className="grid gap-2">{rows.map(row=><button key={row.id} type="button" onClick={()=>setSelectedId(row.id)} className={`grid gap-3 rounded-2xl border p-4 text-start transition ${selectedId===row.id?"border-ink bg-paper shadow-sm":"border-line bg-paper/70 hover:bg-paper"}`}><div className="flex items-start justify-between gap-3"><div className="min-w-0"><strong className="block truncate">{row.businessName}</strong><span className="mt-1 block text-xs text-muted">{row.city||"—"} · {row.email||row.phone||"—"}</span></div><StatusPill status={row.status} lang={lang}/></div><div className="flex flex-wrap gap-2 text-xs text-muted"><span>{new Date(row.submittedAt).toLocaleDateString(lang==="ar"?"ar-SA":"en-US")}</span><span>• {row.isPublished?copy.published:copy.draft}</span></div></button>)}{!rows.length?<div className="grid min-h-72 place-items-center p-8 text-center text-sm text-muted">{copy.empty}</div>:null}</div></div>{selected?<aside className="grid content-start gap-5 rounded-3xl border border-line bg-paper p-5 lg:sticky lg:top-5 lg:h-fit"><div className="flex items-start justify-between gap-4"><div><p className="text-xs text-muted">{copy.details}</p><h2 className="mt-1 font-display text-2xl font-semibold">{selected.businessName}</h2><p className="mt-1 text-sm text-muted">{selected.businessNameEn||selected.city||"—"}</p></div><StatusPill status={selected.status} lang={lang}/></div><div className="grid gap-2 rounded-2xl border border-line bg-sand/20 p-4 text-sm"><Info label={copy.business} value={selected.businessName}/><Info label={copy.city} value={selected.city||"—"}/><Info label={copy.phone} value={selected.phone||"—"} dir="ltr"/><Info label={copy.email} value={selected.email||"—"} dir="ltr"/><Info label={copy.submitted} value={new Date(selected.submittedAt).toLocaleString(lang==="ar"?"ar-SA":"en-US")}/></div><div className="flex flex-wrap gap-2">{selected.phone?<a href={`tel:${selected.phone.replace(/[^0-9+]/g,"")}`} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-line px-3 text-sm"><Phone className="size-4"/>{copy.call}</a>:null}{selected.phone?<a href={`https://wa.me/${selected.phone.replace(/[^0-9]/g,"")}`} target="_blank" rel="noreferrer" className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-line px-3 text-sm"><MessageCircle className="size-4"/>{copy.whatsapp}</a>:null}{selected.email?<a href={`mailto:${selected.email}`} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-line px-3 text-sm"><Mail className="size-4"/>{copy.emailAction}</a>:null}</div><div className="grid gap-3 rounded-2xl border border-line p-4"><div className="flex items-center gap-2 text-sm font-semibold"><UserCheck className="size-4"/>{copy.status}</div><div className="grid grid-cols-2 gap-2"><div className="rounded-xl bg-sand/40 p-3"><span className="block text-xs text-muted">{copy.active}</span><strong className="text-sm">{selected.isActive?"✓":"—"}</strong></div><div className="rounded-xl bg-sand/40 p-3"><span className="block text-xs text-muted">{selected.isPublished?copy.published:copy.draft}</span><strong className="text-sm">{selected.isPublished?"✓":"—"}</strong></div></div><div className="flex flex-wrap gap-2"><a href={selected.menuUrl} target="_blank" rel="noreferrer" className="inline-flex min-h-10 items-center gap-2 rounded-xl border border-line px-3 text-sm"><ExternalLink className="size-4"/>{copy.menu}</a><Button variant="outline" type="button" onClick={()=>void copyMenu()}><Copy className="size-4"/>{copied?copy.copied:copy.copy}</Button></div></div><div className="grid gap-2"><label className="text-sm font-medium">{copy.notes}</label><textarea value={notes} onChange={e=>setNotes(e.target.value)} placeholder={copy.notesPlaceholder} className="min-h-24 rounded-xl border border-line bg-paper p-3 text-sm leading-6 outline-none focus:ring-2 focus:ring-ink/20"/></div>{selected.status==="pending"?<div className="grid gap-2 sm:grid-cols-2"><Button disabled={saving} onClick={()=>void setCustomerStatus("approved")}><CheckCircle2 className="size-4"/>{saving?"…":copy.approve}</Button><Button variant="outline" disabled={saving} onClick={()=>void setCustomerStatus("rejected")}><XCircle className="size-4"/>{copy.reject}</Button></div>:<div className="rounded-xl bg-sand/40 p-3 text-sm">{selected.status==="approved"?copy.approvedMessage:copy.rejectedMessage}</div>}</aside>:<div className="grid min-h-80 place-items-center rounded-3xl border border-dashed border-line bg-paper p-8 text-sm text-muted">{copy.empty}</div>}</section></main> }
function StatusPill({status,lang}:{status:PlatformCustomerRequest["status"];lang:"ar"|"en"}){return <span className="shrink-0 rounded-full bg-sand px-2.5 py-1 text-xs font-medium">{labels[lang][status]}</span>}
function Metric({label,value,active,onClick}:{label:string;value:number;active:boolean;onClick:()=>void}){return <button type="button" onClick={onClick} className={`grid gap-1 rounded-2xl border bg-paper p-4 text-start ${active?"border-ink ring-1 ring-ink/10":"border-line"}`}><span className="text-xs text-muted">{label}</span><strong className="text-2xl">{value.toLocaleString()}</strong></button>}
function Info({label,value,dir}:{label:string;value:string;dir?:"ltr"|"rtl"}){return <div className="flex items-start justify-between gap-4"><span className="text-muted">{label}</span><span className="max-w-[70%] break-words text-end" dir={dir}>{value}</span></div>}
